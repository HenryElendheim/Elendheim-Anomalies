<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The window starts on the deepest background colour, which means the launch
         frame is already dark and the splash fades in without a white flash. -->
    <style name="Theme.Elendheim" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
