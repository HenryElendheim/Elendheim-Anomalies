Elendheim Anomalies finds creatures around wherever you happen to be, catches them with a
skill based throw, and keeps them in a Codex you can export to a file at any time.

Everything runs on the device. No account, no server, no connection needed.

Install the APK below. It signs the same way every release, so it installs straight over
the version already on the phone.
