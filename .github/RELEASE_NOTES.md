Elendheim Anomalies finds creatures around wherever you are, catches them with a skill
based throw, and keeps the collection in a Codex you can export to a file.

Everything runs on the device. No account, no server, no connection needed.

Install the APK below. Every release signs the same way, so it goes straight over the
version already on your phone.
